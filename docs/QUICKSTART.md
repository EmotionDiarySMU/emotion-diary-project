# 감정 일기장 실행 가이드 🚀

## 빠른 시작 (Quick Start)

### 1단계: 데이터베이스 비밀번호 설정

다음 파일들에서 MySQL 비밀번호를 수정하세요:

1. **DatabaseUtil.java**
```bash
파일 위치: src/main/java/com/diary/emotion/model/DatabaseUtil.java
수정 라인: private static final String DB_PASSWORD = "본인의_비밀번호";
```

2. **UserDAO.java**
```bash
파일 위치: src/main/java/com/diary/emotion/model/UserDAO.java
수정 라인: private static final String DB_PASSWORD = "본인의_비밀번호";
```

3. **StatisticsDAO.java**
```bash
파일 위치: src/main/java/com/diary/emotion/model/StatisticsDAO.java
수정 라인: private static final String DB_PASSWORD = "본인의_비밀번호";
```

### 2단계: 테스트 데이터 추가 (선택사항)

테스트를 위해 샘플 데이터를 추가하려면:

```bash
# MySQL에 접속하여 실행
mysql -u root -p < test_data.sql

# 또는 MySQL 클라이언트에서
mysql -u root -p
source test_data.sql;
```

이렇게 하면 다음 테스트 계정이 생성됩니다:
- **아이디**: testuser
- **비밀번호**: test123

### 3단계: 애플리케이션 실행

#### 방법 1: Maven 사용 (권장)
```bash
cd /Users/iee12/IdeaProjects/emotion-diary-project
mvn clean compile exec:java
```

#### 방법 2: IDE 사용
1. IntelliJ IDEA에서 프로젝트 열기
2. `src/main/java/com/diary/emotion/AppLauncher.java` 찾기
3. `main` 메소드 옆의 실행 버튼 클릭 ▶️

## 기능별 사용 가이드

### 🔐 로그인 / 회원가입

#### 회원가입
1. 애플리케이션 실행 시 로그인 화면이 나타남
2. **"회원가입"** 버튼 클릭
3. 아이디 입력 (3~20자)
4. 비밀번호 입력 (4~20자)
5. 비밀번호 확인 입력
6. **"회원가입"** 버튼 클릭
7. 성공 메시지 확인 후 로그인 화면으로 이동

#### 로그인
1. 아이디 입력
2. 비밀번호 입력
3. **"로그인"** 버튼 클릭 (또는 Enter 키)
4. 로그인 성공 시 메인 화면으로 이동

### ✍️ 일기 쓰기 (구현 예정)

1. 메인 화면에서 **"일기 쓰기"** 탭 클릭
2. 제목 입력
3. 내용 입력
4. 감정 선택 (최대 4개)
   - 😊 행복, 😆 신남, 😍 설렘, 😌 편안
   - 😂 재미, 🤗 고마움, 😢 슬픔, 😠 분노
   - 😰 불안, 😅 민망, 😧 당황, 😔 미안함
5. 각 감정의 수치 설정 (0~100)
6. 스트레스 수치 설정 (0~100)
7. **"저장"** 버튼 클릭

### 📖 일기 열람 (구현 예정)

1. 메인 화면에서 **"열람"** 탭 클릭
2. 일기 목록에서 읽고 싶은 일기 선택
3. 검색 기능:
   - **제목 검색**: 검색어 입력 후 검색
   - **날짜 검색**: 시작일/종료일 선택
4. 정렬: 날짜 오름차순/내림차순
5. **수정**: 일기 선택 후 "수정" 버튼
6. **삭제**: 일기 선택 후 "삭제" 버튼

### 📊 통계 확인 (현재 구현됨)

1. 메인 화면에서 **"통계"** 탭 클릭
2. 기간 선택:
   - **주간**: 특정 주의 통계
   - **월간**: 특정 월의 통계
   - **연간**: 특정 연도의 통계
3. 확인 가능한 정보:
   - **감정별 평균**: 긍정/부정 감정 막대 차트
   - **스트레스 추이**: 기간별 스트레스 꺾은선 그래프
   - **평균 스트레스**: 선택한 기간의 평균 스트레스 수치

## 문제 해결 (Troubleshooting)

### Q1: "데이터베이스 초기화 실패" 메시지가 나옵니다.
**A**: MySQL 서버가 실행 중인지 확인하세요.
```bash
# macOS
brew services start mysql

# 또는
mysql.server start
```

### Q2: "Access denied for user" 오류가 발생합니다.
**A**: DB 비밀번호가 올바른지 확인하세요.
- DatabaseUtil.java
- UserDAO.java
- StatisticsDAO.java

### Q3: 로그인이 안 됩니다.
**A**: 
1. 회원가입을 먼저 했는지 확인
2. 테스트 데이터를 추가했다면 testuser/test123 사용
3. MySQL에서 직접 확인:
```sql
USE emotion_diary;
SELECT * FROM user;
```

### Q4: 통계 화면이 비어 있습니다.
**A**: 
1. 로그인이 되어 있는지 확인
2. 해당 사용자의 일기 데이터가 있는지 확인
3. 테스트 데이터를 추가하려면 `test_data.sql` 실행

### Q5: 컴파일 오류가 발생합니다.
**A**: 
```bash
# Maven 의존성 다시 다운로드
mvn clean install

# IntelliJ에서는
# File > Invalidate Caches / Restart
```

## 개발자 정보

### 프로젝트 구조
```
src/main/java/com/diary/emotion/
├── AppLauncher.java           # 진입점
├── model/                     # 데이터 계층
│   ├── DatabaseUtil.java
│   ├── UserDAO.java
│   └── StatisticsDAO.java
├── view/                      # UI 계층
│   ├── LoginView.java
│   ├── SignUpView.java
│   ├── MainApplication.java
│   └── StatisticsView.java
├── controller/                # 비즈니스 로직
│   ├── LoginController.java
│   ├── SignUpController.java
│   └── StatisticsController.java
└── util/                      # 유틸리티
    └── Session.java
```

### 다음 단계 구현 예정
- [ ] 일기 쓰기 기능
- [ ] 일기 열람 및 수정 기능
- [ ] 통계 데이터 완성 (감정/스트레스 차트)
- [ ] 비밀번호 암호화

### 상세 문서
- 📘 [프로젝트 개요](01_PROJECT_OVERVIEW.md)
- 📗 [API 레퍼런스](02_API_REFERENCE.md)
- 📙 [개발 가이드](03_DEVELOPMENT_GUIDE.md)
- 📕 [TODO 리스트](04_TODO_LIST.md)
- 📔 [데이터베이스 스키마](05_DATABASE_SCHEMA.md)
- 📓 [현재 상태 보고서](06_CURRENT_STATUS_REPORT.md)

## 연락처

문제가 발생하면 다음 문서를 참고하세요:
1. `docs/03_DEVELOPMENT_GUIDE.md` - FAQ 섹션
2. `docs/06_CURRENT_STATUS_REPORT.md` - 알려진 문제점

---

**마지막 업데이트**: 2025년 11월 18일

